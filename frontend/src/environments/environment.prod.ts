// Placeholder — sobrescrito por scripts/set-env.js (a partir da env var
// API_URL) sempre que `npm run build:prod` / `npm run start:prod` roda.
// Só fica com este valor se alguém rodar `ng build --configuration
// production` direto, sem passar pelo script.
export const environment = {
  production: true,
  apiUrl: 'https://backend-production-85840.up.railway.app',
};
