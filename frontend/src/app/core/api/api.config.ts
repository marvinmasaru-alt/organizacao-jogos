/**
 * URL base da API. TODO: mover para environment files (environment.ts /
 * environment.prod.ts) quando o provedor de deploy for definido.
 */
export const API_BASE_URL = 'http://localhost:3000';
